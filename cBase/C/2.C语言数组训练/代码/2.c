/*
2、练习：创建一个长度为100的整形数组，分别给元素值为
50,51,52… …98,99,0,1,2,3… … 47,48,49
*/

#include <stdio.h>

int main(void)
{
	//数组a的最后一个元素为a[99]，不存在a[100]这个元素
	int a[100];
	int i;

	for (i=0; i<50; i++)
	{
		a[i]=i+50;
		a[i+50]=i;
	}

	return 0;
}