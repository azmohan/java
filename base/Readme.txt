Sort
Bubble sort:
Most simple method.

SimpleDateFormat

ArrayList and Vector are based on array which stored datas.

direct use index access them

access fast and insert or remove slowly

LinkedList two oriented-list stored datas

insert or remove fast and access slowly


HashTable uses enumeration, HashMap uses iterator


多线程同步是为了能够保证关键数据在单位时间内只能有一个线程操作，
保证数据的同步性，安全性。


sleep()是线程中的一个方法，该方法用于控制自身线程的流程，当兵
执行sleep()方法时，此线程会睡眠。不会释放对象锁。

wait()方法是Object类中的一个方法，该方法用于让一个线程处于等待状态并释放
对象锁。当该线程想要再次执行时需要调用notify()方法唤醒此钱程.

synchronized方法持有包含它对象的对象锁。

java反射机制
在运行状态中，对于任意一个类，都能够知道这个类的所有属性和方法。
对于任何一个对象，都能够调用它的任意一个方法和属性。

atomicity consistency isolation durability

面向过程是分析出解决问题所需要的步骤，然后用函数把这些步骤一步一步地实现。
面向对象是把构成问题的事务分解成各个对象，建立对象描述某个事物在整个解决
问题的步骤中的行为。

进程是系统进行资源分配和调度的一个独立单位，线程是cpu调度和分派的基本单位。

java是一门技术，它由四个方面组成:java编程语言，java类文件格式，java虚拟机和
java应用程序接口。
adapter

jvm有两种机制：
1.类装载子系统：装载具有适合名称的类或接口。
2.执行引擎：负责执行包含在已装载的类或接口中的指令。

java代码编译和执行一整个过程。
jvm内存管理及垃圾回收机制。

java源码编译机制
类加载机制
类执行机制


