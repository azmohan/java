import java.util.List;
import java.util.ArrayList;
public class Test {
    public static void main(String []args) {
        Cow cow = new Cow();
        List childList<Child> = new ArrayList<Child>();
        List sumList<Child> = new ArrayList<Child>();
        for(int i=20;i>0;i--) {
            Child child = cow.fork(i);
            childList.add(child);
        }

        for(
    }

    private List countCows()
}
